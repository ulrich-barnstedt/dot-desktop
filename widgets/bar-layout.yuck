(include "./widgets/bar/apps.yuck")
(include "./widgets/bar/other.yuck")
(include "./widgets/bar/power.yuck")

(defwidget bar-layout []
    (box
        :orientation "v"
        :spacing 15
        :space-evenly false
        :class "pad-top pad-left-sm"
        (power-buttons)
        (update-button)
        (app-launchers)
    )
)
